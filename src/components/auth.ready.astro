---
const isAuthenticated = false;
---
{ isAuthenticated ? (
<button>😀</button>
) : (
<button>🫥</button>
) }

<style>
    button {
        background: none;
        border: none;
        cursor: pointer;
        margin: 0;
        padding: 0;
    }
</style>